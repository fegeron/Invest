
Функция PingGetPing(Запрос)
	
	ДанныеОтвета = Новый Структура("Result", "Ok");
	
	СтрокаJSONОтвет = РаботаСHTTP.СформироватьТекстЗапроса(ДанныеОтвета);
	Ответ = РаботаСHTTP.СформироватьHTTPСервисОтвет(200, СтрокаJSONОтвет);
	
	Возврат Ответ;
	
КонецФункции


Функция PingGetPing(Запрос)
	
	ДанныеОтвета = Новый Структура("Result", "Ok");
	
	СтрокаJSONОтвет = РаботаСHTTP.СформироватьТекстЗапроса();
	Ответ = Новый HTTPСервисОтвет(200);
	Возврат Ответ;
	
КонецФункции
